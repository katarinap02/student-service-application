<Window x:Class="GUI.View.Show.ShowStudents1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI.View.Show"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="ShowStudents1" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <ImageBrush/>
        </Grid.Background>


        <Grid.RowDefinitions>
            <RowDefinition Height="*" />

            <RowDefinition Height="Auto" />

        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGridShowStudents1" IsReadOnly="True" AutoGenerateColumns="False" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="NAME" Width="*" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="SURNAME" Width="*" Binding="{Binding Surname}"/>
                <DataGridTextColumn Header="YEAR" Width="*" Binding="{Binding StYear}"/>
                <DataGridTextColumn Header="STATUS" Width="*" Binding="{Binding StudentStatus}"/>
                <DataGridTextColumn Header="AVERAGE" Width="*" Binding="{Binding Average}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
